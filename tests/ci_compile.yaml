# CI-only config: imports the main abcdesp.yaml but overrides
# external_components to use the local checkout instead of the git URL.
packages:
  base: !include ../abcdesp.yaml

external_components:
  - source:
      type: local
      path: components
    components: [abcdesp]
